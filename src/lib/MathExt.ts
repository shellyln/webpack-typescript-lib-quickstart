

/**
 * Math class.
 */
export class MathExt {
    /**
     * Add two numbers.
     * @param x left operand.
     * @param y right operand.
     */
    public add(x: number, y: number): number {
        return x + y;
    }
}

